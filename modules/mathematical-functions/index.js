const add = (a, b) => a + b;

const multiply = (a, b) => a * b;

const substract = (a, b) => a - b;

const divide = (a, b) => a / b;

console.log("SDK test msg 😍");

module.exports = { add, multiply, substract, divide };
